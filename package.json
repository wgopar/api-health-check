{
  "name": "new-agent-temp",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "dev": "bun run --env-file=.env.development --hot src/index.ts",
    "start": "bun run --env-file=.env.production src/index.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@lucid-agents/agent-kit-hono": "latest",
    "@lucid-agents/agent-kit-identity": "^1.2.1",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "bun-types": "^1.3.0",
    "typescript": "^5.9.2"
  }
}
